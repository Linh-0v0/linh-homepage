import React from "react";
import styled from "styled-components";

const Section = styled.div`
  position: relative;
  height: 100vh;
  width: 87vw;
  overflow: hidden;
  margin: 0 auto;
`;

const SchoolProjects = () => {
  return <Section>SchoolProjects</Section>;
};

export default SchoolProjects;
